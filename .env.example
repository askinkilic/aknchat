# Azure OpenAI Yapılandırması
## Bu dosyayı .env olarak kopyalayın ve değerleri doldurun

# Azure OpenAI endpoint URL'iniz (örn: https://YOUR-RESOURCE-NAME.openai.azure.com)
AZURE_OPENAI_ENDPOINT=

# Azure OpenAI API anahtarınız
AZURE_OPENAI_API_KEY=

# Deployment adınız (örn: MODEL)
AZURE_OPENAI_DEPLOYMENT=MODEL

# API versiyonu (kod ile uyumlu)
AZURE_OPENAI_API_VERSION=2025-01-01-preview

# Server portu (opsiyonel, varsayılan: 3000)
PORT=3000

# Session (cookie) gizli anahtar
SESSION_SECRET=change_me

# Azure AD B2C Yapılandırması
# Tenant kısa adı (örn: contosob2c)
B2C_TENANT=
# Sign-up/Sign-in user flow/policy adı
B2C_POLICY_SIGNIN=B2C_1_signupsignin
# Uygulama (Client) ID
B2C_CLIENT_ID=
# Client Secret (Web app için gerekli)
B2C_CLIENT_SECRET=
# Redirect URI (Azure Portal > Authentication ile aynı olmalı)
B2C_REDIRECT_URI=http://localhost:3000/auth/redirect
# Logout sonrası yönlendirme
B2C_LOGOUT_REDIRECT_URI=http://localhost:3000/

